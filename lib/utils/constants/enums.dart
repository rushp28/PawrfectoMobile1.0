// Enum Constants

// Text Size Enum
enum TextSizes { small, medium, large }

// Order Status Enum
enum OrderStatus { processing, shipped, delivered }

// Payment Method Enum
enum PaymentMethods { paypal, googlePay, applePay, visa, masterCrd, creditCard, paystack, razorPay, paytm }